[build] 
  publish = "dist"
  command = ""

[dev] 
  publish = "dist"
  command = ""

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
